import styles from '@/styles/components/header.module.scss';

const Header = () => {
  return (
    <header className={styles.header}>
      <h1>Death Star Corp</h1>
    </header>
  );
};

export default Header;
